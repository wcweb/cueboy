package us.wcweb.Cueboy.view.components {
	import com.bit101.components.List;

	import flash.display.DisplayObjectContainer;

	/**
	 * @author wcweb
	 */
	public class CueList extends List {
		public function CueList(parent : DisplayObjectContainer = null, xpos : Number = 0, ypos : Number = 0, items : Array = null) {
			super(parent, xpos, ypos, items);
		}
	}
}
